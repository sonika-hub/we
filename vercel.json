{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @workspace/williamest run build:vercel",
  "outputDirectory": "artifacts/williamest/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
